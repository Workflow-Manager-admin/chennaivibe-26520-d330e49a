import React from "react";

// PUBLIC_INTERFACE
/**
 * InteractiveMap
 * Visualizes experiences and events across Chennai on a monochrome map.
 * Props (stub): locations (array), onMarkerClick (fn)
 */
function InteractiveMap(props) {
  return (
    <section>
      <h3>Interactive Map</h3>
      <div>[ Map feature will be integrated here ]</div>
    </section>
  );
}

export default InteractiveMap;
