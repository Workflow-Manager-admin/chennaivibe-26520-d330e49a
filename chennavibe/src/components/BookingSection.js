import React from "react";

// PUBLIC_INTERFACE
/**
 * BookingSection
 * Provides experience booking and payment UI.
 * Props (stub): experience (object), user (object), onConfirm (fn)
 */
function BookingSection(props) {
  return (
    <section>
      <h3>Booking & Payment</h3>
      <div>[ Booking form/widget placeholder ]</div>
    </section>
  );
}

export default BookingSection;
