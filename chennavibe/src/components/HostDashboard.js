import React from "react";

// PUBLIC_INTERFACE
/**
 * HostDashboard
 * Host management dashboard—manage listings, bookings, reviews, analytics.
 * Props (stub): hostData (object), onAction (fn)
 */
function HostDashboard(props) {
  return (
    <section>
      <h3>Host Dashboard</h3>
      <div>[ Host dashboard placeholder ]</div>
    </section>
  );
}

export default HostDashboard;
