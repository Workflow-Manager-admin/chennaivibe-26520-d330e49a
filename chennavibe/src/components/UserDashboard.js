import React from "react";

// PUBLIC_INTERFACE
/**
 * UserDashboard
 * User dashboard—bookings, wishlist, activity overview, profile.
 * Props (stub): userData (object), onAction (fn)
 */
function UserDashboard(props) {
  return (
    <section>
      <h3>User Dashboard</h3>
      <div>[ User dashboard placeholder ]</div>
    </section>
  );
}

export default UserDashboard;
