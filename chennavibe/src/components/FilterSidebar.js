import React from "react";

// PUBLIC_INTERFACE
/**
 * FilterSidebar
 * Sidebar UI for experience filtering (duration, price, theme, etc).
 * Props (stub): filters (object), onChange (fn)
 */
function FilterSidebar(props) {
  return (
    <aside>
      <h3>Filters</h3>
      <div>[ Filter options go here ]</div>
    </aside>
  );
}

export default FilterSidebar;
